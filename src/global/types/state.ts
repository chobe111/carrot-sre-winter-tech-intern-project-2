export enum State {
  PENDING = 'pending',
  AVAILABLE = 'available',
}

export enum CidrState {
  ASSOCIATING = 'associating',
  ASSOCIATED = 'associated',
  DISASSOCIATING = 'disassociationg',
  FAILING = 'failing',
  FAILED = 'failed',
  EMPTY = '',
}
